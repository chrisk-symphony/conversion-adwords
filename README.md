# Symphony Commerce / Google Adwords Conversion Pixel

Use:

var adwordsConversion = require("adwords-conversion")({
	conversionId: CONVERSION_ID,
	conversionLabel: CONVERSION_LABEL,
	remarketingOnly: REMARKETING_ONLY (Optional)
}, ORDER_REVENUE);
